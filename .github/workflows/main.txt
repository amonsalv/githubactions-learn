name: Test

on: [push]


jobs:
    test-build:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v4.2.0

              # This is the step that uses the composite action
            - name: ls
              uses: ./.github/actions/my-action
              with:
                  message: "Ana"