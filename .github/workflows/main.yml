name: Test

on: [push]


jobs:
    test-build:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: checkout@v4.2.0

            - name: ls
              run: | # | es para ejecutar varios comandos #  Dar permisos de ejecución # Ejecutar script
               chmod +x script.sh 
                ./script.sh 